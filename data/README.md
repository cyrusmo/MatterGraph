# `data/`

- **`demo/`** — small JSON/CSV samples for the API and examples (safe to version).
- **`schemas/`** — JSON Schemas for core objects (for interchange and forms).

Do not commit large third-party dataset dumps. Use a local or cloud cache path (see `.gitignore`).
